# S-VHS Releases

This branch serves versioned copies of
[`s-vhs.sh`](https://github.com/dimk90/s-vhs) through GitHub Pages.

- Use a pinned `vX.Y.Z` file for reproducible recordings.
- `latest` follows the newest stable release. Released version files must never be changed.
